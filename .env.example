# 選填：設定後可讓「翻譯成繁體中文」功能穩定運作（見 README「翻譯功能」章節）
# 不設定的話，網站仍可運作，但會使用免費、無需註冊的 MyMemory 翻譯，配額很小，
# 常常會翻譯失敗而顯示英文原文。

AZURE_TRANSLATOR_KEY=
AZURE_TRANSLATOR_REGION=

# 選填：設定後才能使用網站上的「新增網站」「新增分類」「取消關注／刪除分類」功能
# （見 README 同名章節）。需要一組有 Contents: Read and write 權限的 GitHub
# fine-grained personal access token。不設定的話，這些功能不會出現（因為沒有地方
# 能永久儲存變更）。

GITHUB_TOKEN=
GITHUB_REPO=

# 必填（才能使用新增/刪除網站或分類）：管理者密碼，用來登入後台管理功能。
# 「新增網站」「新增分類」等按鈕一直都看得到；沒有設定這組密碼的話，點擊後會清楚
# 顯示「尚未設定管理密碼」的說明，而不是沒有反應。設定後，網站左下角會出現
# 「管理者登入」，輸入這組密碼即可登入（登入狀態存在瀏覽器的 HttpOnly cookie，
# 這組密碼本身永遠不會傳到瀏覽器可讀取的地方）。

ADMIN_PASSWORD=

# 選填但建議設定：新增/刪除、登出等管理 API 的同源（CSRF）保護會比對請求的
# Origin，優先使用這裡設定的網址；沒設定時會退回 Vercel 自動提供的
# VERCEL_PROJECT_PRODUCTION_URL / VERCEL_URL（多數情況已經夠用）。若你的正式站
# 用自訂網域，或想確保萬無一失，請設成你網站的完整網址（見 README「同源保護 /
# CSRF」章節）。本機開發固定允許 http://localhost:3000，不受這個變數影響。

NEXT_PUBLIC_SITE_URL=
